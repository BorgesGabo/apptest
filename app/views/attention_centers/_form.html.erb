<%= form_with(model: attention_center, local: true) do |form| %>
  <% if attention_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attention_center.errors.count, "error") %> prohibited this attention_center from being saved:</h2>

      <ul>
      <% attention_center.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_area :code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_area :phone_number %>
  </div>

  <div class="field">
    <%= form.label :picture %>
    <%= form.text_area :picture %>
  </div>

  <div class="field">
    <%= form.label :schedule %>
    <%= form.text_area :schedule %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
